# Build output
/acli-plus
/bin/
/dist/

# Local, non-committed credentials never live in the repo, but guard anyway
credentials.yaml

# OS / editor
.DS_Store
.idea/

# Local dev tooling (OpenSpec workflow + Claude Code) — not part of the shipped tool
/openspec/
/.claude/
