# Copy this file to `.publish.env` (which is gitignored) and set your token.
# NEVER commit the real token.
#
#   cp .publish.env.example .publish.env
#   # then edit .publish.env and paste your token
#
# Token scope: api  (role Maintainer) — used to build & publish releases.
GITLAB_TOKEN=glpat-REPLACE_ME
